# outputs/

Generated artifacts produced when running the notebooks, for example:

- serialized fuzzy rule bases (`*.pkl`),
- intermediate / cleaned datasets exported as `.xlsx`.

These files are **not version-controlled** (`*.pkl` and `*.xlsx` are listed in
`.gitignore`); they are reproducible by re-running the notebooks. The folder is
kept in the repository so the expected output location exists.
